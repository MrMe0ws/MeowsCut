<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Индикатор ожидания: незамкнутая дуга, вращающаяся вокруг своего центра.
        Дуга, а не бегущие точки, — по ней видно, что приложение живо, и она
        занимает ровно столько же места, сколько кнопка, которую заменяет.
    -->
    <Style x:Key="Spinner" TargetType="Path">
        <Setter Property="Width" Value="22" />
        <Setter Property="Height" Value="22" />
        <Setter Property="Stroke" Value="{DynamicResource Brush.Accent}" />
        <Setter Property="StrokeThickness" Value="2.4" />
        <Setter Property="StrokeStartLineCap" Value="Round" />
        <Setter Property="StrokeEndLineCap" Value="Round" />
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <!-- Дуга в 270°: разрыв показывает вращение, сплошное кольцо выглядело бы неподвижным -->
        <Setter Property="Data" Value="M 11,2 A 9,9 0 1 1 2,11" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <RotateTransform Angle="0" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <!-- Анимация только пока элемент на экране: спрятанный спиннер не должен крутиться -->
            <Trigger Property="Visibility" Value="Visible">
                <Trigger.EnterActions>
                    <BeginStoryboard Name="SpinStoryboard">
                        <Storyboard>
                            <DoubleAnimation RepeatBehavior="Forever"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                             From="0"
                                             To="360"
                                             Duration="0:0:1.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <StopStoryboard BeginStoryboardName="SpinStoryboard" />
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
